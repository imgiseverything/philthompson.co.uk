name: deploy-to-production-ssh
on:
  push:
    branches:
      - master
jobs:
  write-to-console:
    runs-on: ubuntu-latest
    steps:
      # 1 TODO lint/test before we deploy
      # 2 Checkout and list files
      - uses: actions/checkout@v3
      - name: List files
        run: ls
      # 3 Deploy to production with SSH/Rsync
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: "just-a-placeholder-so-we-dont-get-errors"
      - name: Adding Known Hosts
        run: ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
      - name: Deploy with rsync
        run: rsync -avz -O --exclude-from='.github/workflows/deploy-ignore.txt' ./ ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/var/www/philthompson.co.uk/
